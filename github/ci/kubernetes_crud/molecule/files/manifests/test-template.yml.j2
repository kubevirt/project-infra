apiVersion: v1
kind: LimitRange
metadata:
  name: test-limit-range
spec:
  limits:
  - default:
      memory: "{{ memory_limit }}"
    defaultRequest:
      memory: "{{ memory_limit }}"
    type: Container