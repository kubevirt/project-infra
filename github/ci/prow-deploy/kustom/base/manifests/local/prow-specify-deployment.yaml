apiVersion: apps/v1
kind: Deployment
metadata:
  name: specify
  labels:
    app: specify
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prow-specify
  template:
    metadata:
      labels:
        app: prow-specify
    spec:
      serviceAccountName: prow-specify
      terminationGracePeriodSeconds: 180
      containers:
        - name: specify
          image: quay.io/kubevirtci/specify:v20240108-87253b59
          imagePullPolicy: IfNotPresent
          args:
            - --github-token-path=/etc/github/oauth
            - --github-endpoint=http://ghproxy
            - --github-endpoint=https://api.github.com
            - --cache-dir=/var/run/cache
            - --jobs-config-base=github/ci/prow-deploy/files/jobs
            - --prow-config-path=github/ci/prow-deploy/kustom/base/configs/current/config/config.yaml
            - --prow-location=https://raw.githubusercontent.com/kubevirt/project-infra/main
          ports:
            - name: http
              containerPort: 9900
          volumeMounts:
            - name: hmac
              mountPath: /etc/webhook
              readOnly: true
            - name: oauth
              mountPath: /etc/github
              readOnly: true
            - name: plugins
              mountPath: /etc/plugins
              readOnly: true
            - name: cache
              mountPath: /var/run/cache
              readOnly: false
      volumes:
        - name: hmac
          secret:
            secretName: hmac-token
        - name: oauth
          secret:
            secretName: oauth-token
        - name: plugins
          configMap:
            name: plugins
        - name: cache
          emptyDir: {}
