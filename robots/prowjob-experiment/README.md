# ProwJob Experiment

An external [hook](https://github.com/kubernetes/test-infra/tree/master/prow/hook) plugin
that runs modified ProwJobs on new PRs.

**Currently supports only Static Presubmits** (configured on a single repo).